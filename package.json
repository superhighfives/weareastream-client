{
  "name": "client",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "css-wipe": "^4.3.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "2.1.8",
    "styled-components": "^4.2.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "postbuild": "react-snap",
    "build:fab": "npm run build && fab-static build",
    "deploy": "yarn build && surge build weareastream.surge.sh",
    "deploy-prod": "yarn build && surge build weareateam.stream"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "@fab/static": "^0.1.4",
    "prettier": "1.16.4",
    "react-snap": "^1.23.0"
  }
}
